# W1 编程 Agent 任务卡（直接粘贴使用）

> **用法**：一次只贴一张卡，亲眼验收通过再贴下一张；每张卡完成就让 Agent `git commit` 一次。报错**原样完整**贴回去，要求它先解释原因再改；同一张卡卡住两三轮，回退上个快照重新描述。
> **项目目录**：就用本文件夹（以后它就是 GitHub 仓库根目录）。
> **你只需要会三件事**：跑它给你的启动命令、把报错贴回去、让它提交快照。

---

## 卡 0 · 项目骨架

```
请帮我初始化一个本地 Python 项目（就在当前文件夹）：
- Python 3.11，生成 requirements.txt（streamlit、openai、faiss-cpu、python-dotenv）
- .gitignore 排除 .env、__pycache__、data/raw/
- 一个最简 app.py：页面标题"成本感知知识库问答"，加一行占位文字
- .env.example 列出需要的变量名（API key、base_url、模型名），只写说明不写真实值
- README.md 写清本地运行步骤
装好依赖，确保 `streamlit run app.py` 能打开页面。我的系统是 macOS（Apple Silicon）。
先不要写任何业务逻辑。
```

**验收**：浏览器打开，看到标题页。

## 卡 1 · 文档入库

```
data/docs/ 目录下有若干 .md/.txt 公开文档。请写一个 ingest.py：
读取所有文档 → 按约 500 字切片、相邻切片重叠 50 字 → 每个切片记录来源文件名
→ 调用 OpenAI 兼容的 embeddings 接口生成向量 → 把向量、切片文本、来源存成本地索引文件。
切片大小和重叠数放 config.py 里。embedding 的 base_url 和模型名从 .env 读。
脚本运行结束打印"共 N 篇文档、M 个切片"。
```

**验收**：运行看到统计数字；把切片参数改成 300 重跑，数字变化、不报错。

## 卡 2 · 问答页（检索 + 生成 + 引用）

```
在 app.py 里实现问答功能：
用户在输入框提问 → 从本地索引检索 top-3 切片 → 系统提示词要求"只依据参考资料回答，
资料不足以回答时就直说不知道" → 调聊天模型流式输出 → 回答下方列出引用的文档名
和每个片段开头约 50 字。
模型用哪一家、哪个型号全部从配置读，暂时只用一个模型。
交互要求：提问后有"检索中…"的加载提示；回答完成后输入框可以连续追问。
```

**验收**：问一个库内事实（如"gpt-5.4 的输入价格是多少"），回答带引用；再故意问一个明显库外的问题（如"今天天气怎么样"），**把它瞎编的行为截屏记进问题日志**——这是 W4 的种子素材。

## 卡 3 · 成本显示

```
每次回答完成后，用 API 返回的 usage 字段，在引用区下方加一行：
"输入 X tokens / 输出 Y tokens / 本次成本 ≈ $0.00ZZ"。
单价表放 config.py，注释写"取自官方定价页，核验日期 2026-09-XX"。
按两档模型准备配置（便宜档/贵档），现在只接通一档，另一档留好配置位。
不要自己估算 token，用接口返回的真实 usage。
```

**验收**：数字出现，且你拿官方单价手算一次能对上。

## 卡 4 · 拒答兜底 v1

```
检索后如果 top-3 相似度都低于阈值，就不调聊天模型，直接显示固定话术：
"知识库中没有找到相关内容。建议查阅官方文档，或转人工支持。"
下方注明"本次未调用大模型，成本 ¥0"。阈值放 config.py。
```

**验收**：库外问题走拒答路径且不再瞎编；库内问题不受影响。把卡 2 记录的瞎编案例拿出来重测，看几个被拦住了。

## 卡 5 · 里程碑收尾

```
帮我做三件收尾：① 更新 README，写清"现在能做什么 / 还不能做什么 / 已知问题"；
② 建 docs/problem-log.md，用表格记录日期/现象/原因/处理，把这几天测出的问题都填进去；
③ 教我用 git 提交本次里程碑（我之后每个里程碑都要能回退）。
```

**验收**：`git log` 有这条记录；README 里写了至少一条已知问题（诚实写边界就是作品集素材）。

---

## 全程守则（来自 wiki《from-idea-to-demo》）

- 一次只加一个功能；**描述交互，不指定实现**（说"回答完成后可以连续追问"，别说"用 session_state 存历史"）
- 给具体例子："问 X 时，回答应引用定价文档里的那一行"
- 永不相信"已修好"，以实际运行为准
- 密钥只放 `.env`；每张卡验收时全局搜一遍代码，确认没有明文 key
- 测试输入故意刁钻：空输入、超长文本、连续追问、中英混杂——测出来的都是问题日志素材
