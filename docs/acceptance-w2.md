# W2 验收报告（ai-native-dev-flow · 三角色执行）

> 流程：需求分级（完整流程）→ Grill（4 问，用户未答按 ASSUMED 推进，贵档经确认）→ 验收标准 AC1-AC6（用户拍板）→ 三角色执行 → 修复闭环 → 复验。
> 产物：需求简报/验收标准见 `docs/spec-w2.md`；本轮 commit：`c41f536`（Writer 首版）→ `9287fef`（修复）→（最终修复见下）。

## 验收标准与结论（Verifier 复验 · 黑盒）

| # | 标准 | 结论 |
|---|------|------|
| AC1 | 简单题 → flash / 综合题 → pro，均显示理由与成本 | ✅ 通过 |
| AC1b | 综合题"对比 DeepSeek 和 Claude 定价并给建议" → v4-pro | ✅ 通过 |
| AC2 | "鸡吃什么" → 拒答、$0、显示理由 | ✅ 通过 |
| AC2b | `--threshold 0.99` → 所有问题走拒答（阈值兜底生效） | ✅ 通过 |
| AC3 | 复杂/不确定题送贵档且不瞎编（"Claude 全家桶哪个最贵"→pro+引用） | ✅ 通过 |
| AC4 | 便宜档失败（伪造模型名）→ 自动降级 pro + "⚠️降级运行"标注 | ✅ 通过 |
| AC4b | base_url 全错 → 不崩溃、清晰中文故障提示、exit 0 | ✅ 通过（首轮 ❌，修复后复验通过） |
| AC5 | ask.py 三模式（flash/pro/route）均输出各自总成本 | ✅ 通过（flash $0.0006 / pro $0.0022 / route $0.0009） |
| AC6 | 回归：9 题 0 瞎编，定价题价格正确带引用 | ✅ 通过 |

**结论：AC1-AC6 全部通过。** 核心差异已量化：同一道题 flash $0.0006 vs pro $0.0022 vs 路由 $0.0009。

## Reviewer 白盒审查与修复闭环

| 问题 | 级别 | 修复 |
|------|------|------|
| 侧边栏路由模式单选是死控件（mode 只展示不生效） | 🔴 | mode 参数化传入 route_decision |
| config.ROUTE_MODE 全局临时改写（线程不安全） | 🟡 | 删全局改写，参数直传 |
| 分类器词表外标签静默走便宜档 | 🟡 | 白名单校验，越界按复杂/不确定送贵档 |
| 分类器无兜底（异常拖垮全系统） | 🟡 | try/except 兜底，保守送贵档 |
| error 路径语义错误（refused=True、成本归零） | 🟡 | 独立 status=error，分类器成本如实计入 |
| app.py 用字符串嗅探判 error + 成本口径与 CLI 不一致 | 🟡 | 结构化 failed 标志 + error 时分类器成本计入 |
| 拒答分支成本不计入累计 / CHAT_MODEL 残留文案 | 🟢 | 已修 |

## 成本

W2 全程 API 费用：Writer 自测 + Verifier 两轮验收 ≈ $0.06，远低于 ¥50 预算红线。

## 遗留（非阻塞，按计划顺延）

- 召回失败 badcase（"27 万 token 计费"答案在语料但未进 top-3）→ **W4 修复**（top_k、查询改写、切片策略）
- "token 是什么"语料空缺 → 补术语表词条（W1 遗留，进 W4 语料作业）
- 阈值 0.30 正式调优 → W4 用 W3 评测数据
- 第二家服务商真降级 → 二期（README 已知边界如实声明）
