# W1 验收报告（直接验收 · 2026-09-01）

> 流程说明：W1 在引入 ai-native-dev-flow 之前已实现，按用户指示免于 grill/分级/三角色，直接进入验收。
> 验收对象：commit `cb62520`（当前构建，app.py 与 ask.py 共用 qa_core 管线）。
> 验收方式：黑盒——通过 ask.py 跑真实 API 调用，只看行为不看实现；证据 = 本次 3 题抽测 + 今日 9 题全量测试（docs/problem-log.md 共 11 条判定）。

## 验收标准与结论

| # | 标准 | 证据 | 结论 |
|---|------|------|------|
| AC1 | 库内事实问题回答正确且带 [编号] 引用，引用可溯源到 data/docs/ | 抽测：Sonnet 4 价格全对（$3/$15 及缓存价）引 anthropic-pricing；9 题测试：Gemini 到期日正确、检索 0.70 高分命中 | ✅ 通过 |
| AC2 | 库外问题不瞎编：阈值拦截（$0）或模型明确说没找到 | 9+3 题共 0 例瞎编；"天气"阈值拦截 $0；"婚纱/Cursor/1M卡住"模型诚实拒答 | ✅ 通过 |
| AC3 | 每条非拒答回答显示输入/输出 token 与估算成本，单价来自已核验单价表 | 每题附 [tokens]/[成本] 行；单价核验于 2026-09-01（config.py，来源官方定价页） | ✅ 通过 |
| AC4 | 问题日志 ≥10 条，每条含现象/归因/处理方向 | 11 条，含"召回失败"与"相似度≠可答性"两个关键归因 | ✅ 通过 |
| AC5 | UI 层：引用来源、成本行、检索得分在浏览器可见 | 管线已被 ask.py 黑盒覆盖；渲染层待用户在 http://localhost:8501 做 30 秒确认 | ⏳ 待用户确认 |

## 结论

**有条件通过**：AC1-AC4 通过；AC5 待用户浏览器确认后 W1 正式关闭（拍板权在用户）。

## 范围核对（计划 vs 实际）

- 提前实现（原属 W2）：拒答阈值兜底、成本显示、prompt caching 单价记录
- 按计划顺延：难度/可答性分类与双档路由、上游故障降级（→W2）；评测集与三臂对比（→W3）；badcase 修复与回归（→W4）
- 已知边界（均已记入问题日志，带复现问题）：检索召回失败 1 例（"27 万 token 计费"答案在语料但未进 top-3）；语料空缺 1 例（"token 定义"术语表未收录）；阈值模糊区证据 3 例（0.2996/0.33/0.55）

## 成本

验收全程 API 费用：$0.0044（9 题）+ $0.0015（3 题抽测）≈ ¥0.05，远低于 ¥50 预算红线。
