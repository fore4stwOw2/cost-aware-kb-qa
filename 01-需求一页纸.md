# 需求澄清一页纸 · Cost-Aware KB-QA（成本感知的知识库问答）

> 用法：这是项目的"宪法"——每周末对照一遍"成功标准"；做完项目后它直接改写成 README 的开头。【】里的内容可以按你的想法替换，其余建议保留。

## 一句话

一个【面向中小企业】的大模型知识库问答工具：自动判断问题难度，简单问题用便宜模型、复杂问题用贵模型，每条回答展示所用模型与成本；库外问题拒绝瞎编，上游故障自动切换——目标是**在回答质量几乎不损失的前提下，显著降低 LLM 调用成本**。

## 谁在什么场景遇到什么困难

- 中小企业想给产品手册 / FAQ 上一个 AI 问答客服。接上大模型后发现：全用旗舰模型效果稳但账单吓人；全用便宜模型，复杂问题答不好。
- 老板 / 客户最常问的一句话："能不能让大多数问题走便宜模型，难的再升级？"
- 市面上没有帮他们**先验证再上线**的东西：路由到底能省多少成本、质量损失多少，没人说得清。这个项目就是把这件事做成可验证的数字。

## 目标与成功标准（每条都可肉眼验证）

1. 库内问题：回答附带引用（来自哪篇文档、哪个片段）
2. 库外问题：明确拒答 + 建议转人工，**不编造**——「不瞎编」是可验收标准，不是感觉
3. 问题难度由便宜模型分类：简单 → 便宜档，复杂 → 贵档
4. 每条回答显示：所用模型、输入/输出 token 数、估算成本
5. 任一上游 API 失败：自动切到另一家，用户无感
6. 评测：30 条用例三臂对比（全便宜 / 全贵 / 路由），产出三个数——路由分类准确率、质量分差、成本节省比例【目标先假设：省 ≥30% 成本、质量下降 ≤10 个百分点；做完用真实数字替换本条】

## 范围（明确不做）

登录 / 多用户 / 生产部署 / 微调 / Agent 工具调用（留二期）/ 任何公司内部数据（语料全用公开材料）

## 核心假设（整个项目就是在验证它）

便宜模型能把"难度分类"做对到足够程度（如 ≥80%），使混合成本显著低于全贵方案。如果做不对——badcase 归因本身就是项目最有价值的部分，不要慌，那是素材不是失败。

## 一段话讲清这个项目（面试开场用，做完填数）

"我在 API 中转公司工作，客户最常纠结的就是模型性价比。我做了一个小工具验证这件事：知识库问答先做难度分类、再路由到不同档位的模型，配了一套 30 条用例的评测集做三臂对比——路由方案比全用旗舰模型省了【X】% 成本，质量只降【Y】个百分点，路由分类准确率【Z】%。"
